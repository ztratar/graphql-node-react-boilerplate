import SignupComplete from './templates/signupComplete';
import ResetPassword from './templates/resetPassword';
import CustomerBillingFailed from './templates/customerBillingFailed';

const emails = {
  SignupComplete,
  ResetPassword,
  CustomerBillingFailed
};

export default emails;
